# Why
- Day 10 turns the previous nine changes into a handoff package that can be demonstrated and extended without reopening planning debt.
- The source plan explicitly calls for demo data, a fixed demo route, a known-issues list, and a Week 3 backlog.
- A final handoff change prevents the MVP from ending in an undocumented “almost done” state.

## What Changes
- Define the demo dataset, demo runbook, and minimum acceptance route for the community MVP.
- Define the known-issues log and the decision backlog for Redis/Celery, hot ranking, and real-time notifications.
- Define the daily close-out rule so the 10-day execution record stays synchronized after delivery.

## Impact
- Adds capability `community-demo-handoff`.
- Depends on `add-community-day-09-external-hot-feed-v1`.
- Produces the final delivery package for the 10-day community rollout.
