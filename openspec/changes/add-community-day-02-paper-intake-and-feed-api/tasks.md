## 1. Start Gate
- [ ] 1.1 Confirm `add-community-day-01-schema-rls-foundation` is approved or treated as the active baseline.
- [ ] 1.2 Mark Day 2 as `In Progress` in `texts/社区打造十天OpenSpec执行索引.md`.

## 2. Delivery Scope
- [ ] 2.1 Define `POST /api/papers/submit` for upload and arXiv-based intake.
- [ ] 2.2 Define `GET /api/papers`, `GET /api/papers/{paper_id}`, and `POST /api/papers/{paper_id}/view`.
- [ ] 2.3 Define the mapping between `papers`, translation tasks, and `paper_assets`.

## 3. Validation And Sync
- [ ] 3.1 Run `openspec validate add-community-day-02-paper-intake-and-feed-api --strict --no-interactive`.
- [ ] 3.2 Update the Day 2 status in `texts/社区打造十天OpenSpec执行索引.md` after all checklist items are done.
