## 1. Session Record Scope
- [x] 1.1 Identify the session changes that need formal OpenSpec coverage.
- [x] 1.2 Map each shipped change to an existing capability instead of creating duplicate capabilities.

## 2. Spec Deltas
- [x] 2.1 Record runtime database wiring requirements for local auth and community persistence.
- [x] 2.2 Record the login identifier contract for email-or-phone sign-in.
- [x] 2.3 Record community-agent timestamp compatibility requirements for MySQL-backed persistence.
- [x] 2.4 Record LaTeX include-resolution behavior for directory targets.
- [x] 2.5 Record the fixed Processing workbench layout and internal log scrolling behavior.

## 3. Validation
- [x] 3.1 Run `openspec validate record-session-runtime-and-processing-fixes --strict --no-interactive`.
