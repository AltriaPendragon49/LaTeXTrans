# Change: Update Project Readmes

## Why
The current root `Readme.md` represents an outdated "Phase 1: MVP Skeleton" state of the project. The system has since evolved significantly, now featuring a robust FastAPI backend with advanced translation configurations, a React frontend with drag-and-drop upload, arXiv fetching, Supabase integration for auth and history, and improved compilation strategies. The root Readme needs to be updated to serve as an accurate, external-facing entry point.
Additionally, the `backend/README.md` and `frontend/README.md` need to be updated to better reflect the current system architecture, available features, and the latest specifications defined in the OpenSpec folder.

## What Changes
- Rewrite the root `Readme.md` to accurately reflect the current system architecture (FastAPI + React + Supabase), update the features list, update Quick Start instructions, and remove outdated roadmap items.
- Update `backend/README.md` to align with the latest backend specifications, API endpoints, and configuration options.
- Update `frontend/README.md` to align with the latest frontend features, UI/UX refinements, and component structures.

## Impact
- Affected specs: None (Documentation only)
- Affected code: `/Readme.md`, `/backend/README.md`, `/frontend/README.md`
